<!DOCTYPE html>
<html>
<head>
  <title>{{title}}</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Roboto:wght@300;400&display=swap" rel="stylesheet">
</head>
<body>
  <div class="container">
    <div class="sidebar">
      <h3>Developer</h3>
      <p>GitHub: <a href="https://github.com/{{githubUsername}}" target="_blank">{{githubUsername}}</a></p>
      {{#if user}}
        <p>Welcome, {{user.username}}!</p>
        <nav>
          <a href="/dashboard">Dashboard</a> |
          <a href="/calendar">Calendar</a> |
          <a href="/logout">Logout</a>
        </nav>
      {{else}}
        <a href="/login">Login</a> | <a href="/register">Register</a>
      {{/if}}
      {{#unless dbConnected}}
        <p class="error">Database offline - limited functionality</p>
      {{/unless}}
    </div>
    <div class="content">
      {{#if success_msg}}<p class="success">{{success_msg}}</p>{{/if}}
      {{#if error_msg}}<p class="error">{{error_msg}}</p>{{/if}}
      {{#if error}}<p class="error">{{error}}</p>{{/if}}
      {{{body}}}
    </div>
  </div>
</body>
</html>